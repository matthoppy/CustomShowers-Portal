name = "customshowers-calls"
main = "index.ts"
compatibility_date = "2024-09-23"

[vars]
SUPABASE_URL = "https://qgfmsyxaccvwmmygtspf.supabase.co"

# Secrets (set via `wrangler secret put` or Cloudflare dashboard):
#   SUPABASE_SERVICE_KEY
#   TWILIO_AUTH_TOKEN   (used to verify webhook signatures)
